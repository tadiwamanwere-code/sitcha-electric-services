# GEMINI_API_KEY — powers the AI chat assistant.
# Optional: with no key (or this placeholder value) the chatbot falls back to
# keyword-matched canned replies, so the site still works. Never expose this to
# the client bundle — it is read server-side only.
GEMINI_API_KEY="YOUR_API_KEY"
